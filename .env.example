# SatNOGS Network API token.
# Find it on your account page after logging in at https://network.satnogs.org
# Copy this file to `.env` (which is gitignored — your token stays out of git) and
# fill it in. Tools load it via:  set -a; source .env; set +a
SATNOGS_API_TOKEN=
